# Ferramentas

Procuradoria Geral do Estado CE: Dívida Ativa expõe 1 ferramenta (todas somente leitura).

### 1. `pge_ce_divida_ativa_consultar`
**Input**: `cpf` (opcional), `cnpj_base` (opcional), `pagina` (opcional), `numero_inscricao` (opcional)

Procuradoria Geral do Estado CE: Dívida Ativa, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Procuradoria Geral do Estado CE: Dívida Ativa
```
